import React from 'react';
import { Typography, Card } from '@arco-design/web-react';

function codeOnline() {
  return (
    <Card style={{ height: '80vh' }}>
      <Typography.Title heading={6}>在线Code</Typography.Title>
    </Card>
  );
}

export default codeOnline;
